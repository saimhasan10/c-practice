#include<stdio.h>
int main()

{
    char source[]={"C programming"};
    char target[50];

    // copy function in string
    strcpy(target,source);

    printf("The source says that: %s\n",source);
    printf("The target also says that: %s",target);





}
